import React from 'react';

export default function Exercise4() {
  return (
    <div>
      <h2>Exercise 4</h2>
      <p>
        We'll be going back to <code>Exercise1.jsx</code> for this one.
      </p>
      <ol>
        <li>In the file <code>Exercise1.test.jsx</code>, create a test for the <code>CoffeeCard</code> component.</li>
        <li>The test should verify an aspect of the <code>CoffeeCard</code>'s rendering, such as if the image is rendered with an <code>alt</code> property, or if the price is formatted correctly.</li>
      </ol>
    </div>
  );
}